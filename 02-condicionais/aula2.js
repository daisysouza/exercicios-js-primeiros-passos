
const numero = 5;

const isNumeroPar = (numero % 2) === 0;

if (isNumeroPar) {
    console.log('O número é par');
} else {
    console.log('o número é impar');
}

// Pega o numero e divide pelo resto da divisão: %

// Para saber se o número é par o resto da divisão deve ser maior que 0

// Sempre o resultado de uma condicional é um booleano (expressão que retorna um booleano)

// !numeroPar - negação